// Beispiel-Skript für CustomCore
// Wird beim Serverstart automatisch geladen (scripts.auto-load: true in config.yml)
// Verfügbares Objekt: Server (siehe ScriptAPI.java für alle Methoden)

Server.log("example.js wurde geladen!");

// Beispiel: Scoreboard-Titel per Skript setzen
// Server.setScoreboardTitle("&b&lSCRIPTED SERVER");

// Beispiel: Nachricht an alle senden
// Server.broadcast("&aDieses Skript läuft jetzt!");

// Beispiel: Spieler-Rang per Skript abfragen
// var rank = Server.getPlayerRank("Notch");
// Server.log("Notch hat den Rang: " + rank);
